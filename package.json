{
  "name": "kantinebot",
  "version": "1.0.0",
  "description": "Scrape the kantine menu",
  "main": "index.ts",
  "scripts": {
    "build": "tsc -p . -w",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^4.5.5"
  },
  "dependencies": {
    "puppeteer": "^13.3.2"
  }
}
